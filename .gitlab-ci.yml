image: node:lts

pages:
  cache:
    paths:
    - node_modules/

  stage: deploy
  script:
  - npm install -g @angular/cli@latest
  - npm install
  - npm run build tp-note
  artifacts:
    paths:
    - public
  only:
  - master
  - pages